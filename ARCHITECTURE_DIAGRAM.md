# Security Architecture Diagram

## System Architecture (After Implementation)

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                                 │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  React Frontend (http://localhost:3000)                      │  │
│  │                                                              │  │
│  │  ┌────────────────────────────────────────────────────┐    │  │
│  │  │  Login Component (Login.js)                        │    │  │
│  │  │  - Email/Password input                           │    │  │
│  │  │  - Signup tab                                     │    │  │
│  │  │  - POST /api/login or /api/register               │    │  │
│  │  │  - Stores JWT token: localStorage.setItem()       │    │  │
│  │  └────────────────────────────────────────────────────┘    │  │
│  │                          ↓                                  │  │
│  │  ┌────────────────────────────────────────────────────┐    │  │
│  │  │  App Component (App.js)                            │    │  │
│  │  │  - useEffect checks localStorage on mount          │    │  │
│  │  │  - Verifies token exists & valid                   │    │  │
│  │  │  - Sets user role (admin or user)                  │    │  │
│  │  │  - Routes to Admin or UserDashboard                │    │  │
│  │  └────────────────────────────────────────────────────┘    │  │
│  │           ↓                         ↓                       │  │
│  │  ┌─────────────────────┐  ┌──────────────────────────┐    │  │
│  │  │ Admin Component     │  │ UserDashboard Component  │    │  │
│  │  │                     │  │                          │    │  │
│  │  │ GET /api/users      │  │ GET /api/profile         │    │  │
│  │  │ POST /api/promote   │  │ POST /api/donate         │    │  │
│  │  │ POST /api/demote    │  │                          │    │  │
│  │  │                     │  │ Axios calls get token    │    │  │
│  │  │ Axios gets token    │  │ via interceptor          │    │  │
│  │  │ via interceptor     │  │                          │    │  │
│  │  └─────────────────────┘  └──────────────────────────┘    │  │
│  │                                                             │  │
│  │  ┌────────────────────────────────────────────────────┐    │  │
│  │  │  Axios Interceptor (axiosConfig.js)                │    │  │
│  │  │                                                    │    │  │
│  │  │  Request Interceptor:                             │    │  │
│  │  │    → Gets token from localStorage                  │    │  │
│  │  │    → Adds header: Authorization: Bearer {token}    │    │  │
│  │  │    → All API calls include token automatically     │    │  │
│  │  │                                                    │    │  │
│  │  │  Response Interceptor:                            │    │  │
│  │  │    → If status 401 (unauthorized)                 │    │  │
│  │  │    → Clear localStorage                           │    │  │
│  │  │    → Redirect to login page                       │    │  │
│  │  └────────────────────────────────────────────────────┘    │  │
│  │                          ↓                                  │  │
│  │  localStorage:                                            │  │
│  │    token: "eyJhbGciOiJIUzI1NiIs..."                      │  │
│  │    user: {email, role, id, name}                         │  │
│  │                                                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↓↑                                     │
│                    HTTPS/TLS (Production)                       │
│                          ↓↑                                     │
└──────────────────────────────────────────────────────────────┘
                          ↓↑
         ┌────────────────────────────────┐
         │   Network (localhost:5000)     │
         │   CORS: localhost:3000 only    │
         └────────────────────────────────┘
                          ↓↑
┌──────────────────────────────────────────────────────────────────┐
│                    EXPRESS SERVER (Node.js)                       │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Middleware Stack (Left to Right)                         │  │
│  │                                                           │  │
│  │  Request → helmet() → cors() → express.json() →          │  │
│  │  → loginLimiter/registerLimiter → authenticate? →        │  │
│  │  → adminOnly? → Route Handler → Response                 │  │
│  │                                                           │  │
│  │  helmet():  Adds security headers                        │  │
│  │  cors():    Restricts origin to localhost:3000           │  │
│  │  Limiters:  Rate limits login (5/15min), register        │  │
│  │  authenticate: Verifies JWT token                        │  │
│  │  adminOnly: Checks req.user.role === "admin"             │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Protected Routes (All require authentication)          │    │
│  │                                                         │    │
│  │  POST /api/register [registerLimiter]                  │    │
│  │    ├─ Validate: email, phone (10 digits), password     │    │
│  │    ├─ Hash password: bcryptjs (10 rounds)              │    │
│  │    ├─ Save user to MongoDB                             │    │
│  │    └─ Generate JWT token                               │    │
│  │                                                         │    │
│  │  POST /api/login [loginLimiter]                        │    │
│  │    ├─ Find user by email                               │    │
│  │    ├─ Compare password with bcrypt hash                │    │
│  │    ├─ Generate JWT token                               │    │
│  │    └─ Return token + user data                         │    │
│  │                                                         │    │
│  │  GET /api/profile [authenticate]                       │    │
│  │    ├─ Verify JWT token                                 │    │
│  │    ├─ Extract userId from token                        │    │
│  │    └─ Return user profile                              │    │
│  │                                                         │    │
│  │  POST /api/donate [authenticate]                       │    │
│  │    ├─ Verify JWT token                                 │    │
│  │    ├─ Validate amount (positive number)                │    │
│  │    ├─ Save donation record                             │    │
│  │    └─ Return success                                   │    │
│  │                                                         │    │
│  │  GET /api/users [authenticate, adminOnly]              │    │
│  │    ├─ Verify JWT token                                 │    │
│  │    ├─ Check role = "admin"                             │    │
│  │    ├─ Return all users (without passwords)             │    │
│  │    └─ Log action to audit trail                        │    │
│  │                                                         │    │
│  │  POST /api/promote [authenticate, adminOnly]           │    │
│  │    ├─ Verify JWT token                                 │    │
│  │    ├─ Check role = "admin"                             │    │
│  │    ├─ Validate email                                   │    │
│  │    ├─ Update user role to "admin"                      │    │
│  │    ├─ Log action: {actionType, performedBy, target}    │    │
│  │    └─ Prevent super admin demotion                     │    │
│  │                                                         │    │
│  │  POST /api/demote [authenticate, adminOnly]            │    │
│  │    ├─ Verify JWT token                                 │    │
│  │    ├─ Check role = "admin"                             │    │
│  │    ├─ Validate email                                   │    │
│  │    ├─ Prevent demotion if super admin                  │    │
│  │    ├─ Update user role to "user"                       │    │
│  │    ├─ Log action: {actionType, performedBy, target}    │    │
│  │    └─ Return success                                   │    │
│  │                                                         │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  JWT Token Structure                                    │    │
│  │                                                         │    │
│  │  Header: {                                              │    │
│  │    "alg": "HS256",                                      │    │
│  │    "typ": "JWT"                                         │    │
│  │  }                                                      │    │
│  │                                                         │    │
│  │  Payload: {                                             │    │
│  │    "userId": "507f1f77bcf86cd799439011",               │    │
│  │    "email": "user@example.com",                         │    │
│  │    "role": "user",                                      │    │
│  │    "iat": 1672531200,                                   │    │
│  │    "exp": 1672617600                                    │    │
│  │  }                                                      │    │
│  │                                                         │    │
│  │  Signature: HMACSHA256(base64(header) + "." +           │    │
│  │                       base64(payload),                  │    │
│  │                       process.env.JWT_SECRET)           │    │
│  │                                                         │    │
│  │  Sent as: Authorization: Bearer eyJhbGciOiJIUzI1NiI... │    │
│  │                                                         │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Environment Variables (.env)                           │    │
│  │                                                         │    │
│  │  NODE_ENV=development                                  │    │
│  │  PORT=5000                                             │    │
│  │  MONGO_URI=mongodb://127.0.0.1:27017/ngo_project       │    │
│  │  JWT_SECRET=your-super-secret-jwt-key                  │    │
│  │  SUPER_ADMIN_EMAIL=admin@gmail.com                     │    │
│  │  SUPER_ADMIN_PASSWORD=admin123456 (hashed on init)    │    │
│  │  CLIENT_URL=http://localhost:3000                      │    │
│  │  PAYPAL_CLIENT_ID=...                                  │    │
│  │  PAYHERE_MERCHANT_ID=...                               │    │
│  │                                                         │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
                          ↓↑
┌──────────────────────────────────────────────────────────────────┐
│                      MONGODB DATABASE                             │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Collection: users                                       │   │
│  │                                                          │   │
│  │  {                                                       │   │
│  │    _id: ObjectId("507f1f77bcf86cd799439011"),           │   │
│  │    name: "John Doe",                                    │   │
│  │    email: "john@example.com" (unique, lowercase),       │   │
│  │    phone: "9876543210" (10 digits),                     │   │
│  │    password: "$2a$10$bZ5TtxK3y9..." (bcrypt hash),      │   │
│  │    role: "user" (enum: user, admin),                    │   │
│  │    donations: [                                         │   │
│  │      {                                                  │   │
│  │        amount: 1000 (Number, positive),                 │   │
│  │        status: "Success" (enum),                        │   │
│  │        date: "2025-01-20T10:00:00Z"                    │   │
│  │      }                                                  │   │
│  │    ],                                                   │   │
│  │    createdAt: "2025-01-20T10:00:00Z"                   │   │
│  │  }                                                      │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Collection: auditlogs                                   │   │
│  │                                                          │   │
│  │  {                                                       │   │
│  │    _id: ObjectId(...),                                  │   │
│  │    actionType: "promote",                               │   │
│  │    performedBy: "admin@gmail.com",                      │   │
│  │    targetUser: "user@example.com",                      │   │
│  │    timestamp: "2025-01-20T10:15:00Z",                   │   │
│  │    status: "success"                                    │   │
│  │  }                                                      │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

---

## Authentication Flow Diagram

```
┌─────────────────┐
│  User Enters    │
│  Credentials    │
└────────┬────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  Frontend: POST /api/login           │
│  Body: {email, password}             │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  Backend: loginLimiter Middleware    │
│  Check: 5 attempts per 15 minutes    │
└────────┬─────────────────────────────┘
         │
         ↓ (Pass or Fail with 429)
┌──────────────────────────────────────┐
│  Backend: Validate Input             │
│  - Check email format                │
│  - Check password length >= 6        │
└────────┬─────────────────────────────┘
         │
         ↓ (Pass or Fail with 400)
┌──────────────────────────────────────┐
│  Backend: Find User in Database      │
│  - Query: User.findOne({email})      │
└────────┬─────────────────────────────┘
         │
         ├─ NOT FOUND ──→ Return 401
         │
         ↓ (Found)
┌──────────────────────────────────────┐
│  Backend: Compare Passwords          │
│  - bcrypt.compare(input, stored)     │
└────────┬─────────────────────────────┘
         │
         ├─ MISMATCH ──→ Return 401
         │
         ↓ (Match)
┌──────────────────────────────────────┐
│  Backend: Generate JWT Token         │
│  jwt.sign({userId, email, role},     │
│           JWT_SECRET,                │
│           {expiresIn: '24h'})         │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  Frontend: Receive Response          │
│  {token, user: {id, email, role}}    │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  Frontend: Store Token               │
│  localStorage.setItem("token", token)│
│  localStorage.setItem("user", user)  │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  Frontend: Redirect to Dashboard     │
│  Check role: admin → AdminDash       │
│            user  → UserDash          │
└──────────────────────────────────────┘
```

---

## Protected API Request Flow

```
┌──────────────────────────────────────┐
│  Frontend Makes API Request          │
│  GET /api/users                      │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  Axios Request Interceptor           │
│  - Get token from localStorage       │
│  - Add header:                       │
│    Authorization: Bearer {token}     │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  Backend: CORS Middleware            │
│  Check: origin === CLIENT_URL        │
└────────┬─────────────────────────────┘
         │
         ├─ BLOCKED ──→ Return 403
         │
         ↓ (Pass)
┌──────────────────────────────────────┐
│  Backend: authenticate Middleware    │
│  - Extract Bearer token              │
│  - jwt.verify(token, JWT_SECRET)     │
│  - Set req.user = {userId, ...}      │
└────────┬─────────────────────────────┘
         │
         ├─ INVALID ──→ Return 401
         ├─ EXPIRED ──→ Return 403
         │
         ↓ (Valid)
┌──────────────────────────────────────┐
│  Backend: adminOnly Middleware       │
│  Check: req.user.role === "admin"    │
└────────┬─────────────────────────────┘
         │
         ├─ NOT ADMIN ──→ Return 403
         │
         ↓ (Admin)
┌──────────────────────────────────────┐
│  Backend: Route Handler              │
│  GET /api/users implementation       │
│  - Query database                    │
│  - Return users (no passwords)       │
│  - Log to audit trail                │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  Frontend: Receive Response          │
│  Status 200: [list of users]         │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  Frontend: Response Interceptor      │
│  Check: status code                  │
└────────┬─────────────────────────────┘
         │
         ├─ 401: Clear token & redirect
         ├─ 403: Redirect to login
         │
         ↓ (200)
┌──────────────────────────────────────┐
│  Frontend: Render Data               │
│  Update state with users             │
└──────────────────────────────────────┘
```

---

## Session Lifecycle Diagram

```
Timeline:

T0: User Registers/Logins
    │
    ├─ Token created with exp: T0 + 24h
    ├─ Stored in localStorage
    └─ User logged in ✓
    
T1 - T24h: User Uses App
    │
    ├─ Refresh page: Token checked on App mount
    ├─ API calls: Token automatically included
    ├─ Token valid: Requests succeed
    └─ User remains logged in ✓
    
T24h: Token Expires
    │
    ├─ API request sent with expired token
    ├─ Backend: jwt.verify() fails
    ├─ Returns 401 Unauthorized
    └─ Response interceptor handles
    
T24h+1s: Session Expires
    │
    ├─ Interceptor clears localStorage
    ├─ User redirected to login page
    ├─ New registration/login required
    └─ New token generated ✓

Logout (Anytime):
    │
    ├─ User clicks Logout
    ├─ Frontend clears localStorage
    ├─ Redirects to login page
    ├─ Token invalidated
    └─ User must re-login
```

---

## Security Layers

```
        User Input
            ↓
    ┌───────────────────┐
    │  Rate Limiting    │  ← Prevents brute force
    └─────────┬─────────┘
              ↓
    ┌───────────────────┐
    │  CORS Check       │  ← Prevents cross-origin attacks
    └─────────┬─────────┘
              ↓
    ┌───────────────────┐
    │  Input Validation │  ← Prevents injection attacks
    └─────────┬─────────┘
              ↓
    ┌───────────────────┐
    │  JWT Verification │  ← Authenticates user
    └─────────┬─────────┘
              ↓
    ┌───────────────────┐
    │  Role Check       │  ← Authorizes action
    └─────────┬─────────┘
              ↓
    ┌───────────────────┐
    │  Business Logic   │  ← Execute safe operation
    └─────────┬─────────┘
              ↓
    ┌───────────────────┐
    │  Audit Logging    │  ← Track for compliance
    └─────────┬─────────┘
              ↓
        Database Store
```

---

## Technology Stack

```
Frontend Layer:
├─ React (User Interface)
├─ Material-UI (Design System)
├─ Axios (HTTP Client)
│  └─ Interceptor (Auto JWT inclusion)
├─ localStorage (Token Storage)
└─ Browser (Client-side execution)

Network Layer:
├─ HTTPS/TLS (Production)
├─ CORS (Cross-Origin Control)
└─ HTTP Headers (Security)

Backend Layer:
├─ Express.js (Web Framework)
├─ Helmet.js (Security Headers)
├─ express-rate-limit (Rate Limiting)
├─ jsonwebtoken (JWT Handling)
├─ bcryptjs (Password Hashing)
├─ email-validator (Input Validation)
└─ dotenv (Configuration)

Database Layer:
├─ MongoDB (NoSQL Database)
├─ Mongoose (ODM)
├─ Schema Validation
├─ Unique Indexes
└─ Audit Logging
```

---

## Data Flow Summary

```
User ←→ Frontend ←→ Axios ←→ Middleware ←→ Routes ←→ Database
       (React)    (Interceptor)  (Auth)    (Handlers)  (MongoDB)
         │           │             │           │           │
         ├─Token     ├─Add         ├─Verify    ├─Validate  ├─Save
         │  Storage  │  Auth       │  JWT      │  Input    │  Hashed
         ├─Session   │  Header     ├─Check     ├─Execute   │  Data
         │  Persist  │             │  Role     │  Logic    │
         └─Logout    └─Handle 401  └─Audit     └─Respond   └─Audit
            Clear        Redirect      Log              Log
```

---

This architecture ensures that:

- ✅ Users are authenticated with JWT tokens
- ✅ Authorization is enforced server-side
- ✅ All data is validated before use
- ✅ Passwords are hashed and never stored plaintext
- ✅ Rate limiting prevents brute force attacks
- ✅ CORS prevents cross-origin attacks
- ✅ Security headers protect against browser attacks
- ✅ Audit trail tracks all admin actions
- ✅ No sensitive data is exposed
- ✅ Sessions expire automatically after 24 hours

**RESULT: Production-Ready Secure Application 🔒**
